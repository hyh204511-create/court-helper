# 法院立案/强执查询助手（court-helper）

## 目标
Edge 插件，辅助操作人员查询人民法院诉讼服务网（zxfw.court.gov.cn）的**立案**与**强制执行**状态，自动识别状态、截图存证、按固定模板写入 Excel，支持导入/导出与软件内查询。

## 核心流程（已与用户确认）
1. **登录全人工**：操作人员手动登录平台（账号/密码/验证码），插件不碰登录表单、不存储凭据，只在登录后工作。
2. 插件检测当前登录账号 → 匹配模板「账号」列对应行 → 批量查询该账号下案件。
3. 立案状态识别：`待审核→审核中`（只记查询时间）；`已立案→立案成功`（截图 + 立案成功时间 + 案号）；`驳回/待补充材料→已驳回`（驳回时间 + 驳回原因 + 截图）。
4. 强执状态识别：`已立案→强执成功`（截图 + 强执成功时间 + 强执案号）；其余同上。
5. 数据落 IndexedDB，按模板格式导出（含图片单元格），也可从模板导入新案件、按账号/客户名/状态查询。

## 模板（立案与强执查询表-模板.xlsx）
单工作表双表块：立案表（表头第 1 行）+ 强执表（表头随立案末行 +5 动态下移）；12 列（原告/被告/账号/密码/状态/成功时间/案号/成功图片/驳回时间/驳回原因/驳回图片/查询时间）；表头加粗浅绿填充；日期列 `mm-dd-yy`；图片用标准嵌入（非 DISPIMG）。真实模板含业务数据，只存本地私有位置；仓库内用脱敏 fixture。

## 约束
- 规格先行（docs/specs/），TDD，原子提交；真实业务数据不进 git/知识库。
- 详细计划见根目录 `.hermes/plans/`。
